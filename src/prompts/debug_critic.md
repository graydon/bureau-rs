# DEBUG · CRITIC

Run `cargo_test`. Identify anything still failing or any test that was loosened to make impl pass. Report via `submit_critique` exactly once. If clean, call `submit_critique` with an EMPTY `issues` list.